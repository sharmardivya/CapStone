"""
LangChain tools. Each specialist agent's LLM decides when to call its tool.

    wikipedia_tool.py  destination facts (Destination Research Agent)
    weather_tool.py    live weather from wttr.in (Weather Intelligence Agent)
    cost_tool.py       daily cost lookup (Budget Estimator Agent)
    leave_tool.py      leave balance lookup in SQLite (HR Leave Agent)
"""
